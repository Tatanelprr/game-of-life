name: Déployer sur GitHub Pages

on:
  push:
    branches: [main]

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      # On récupère l'URL ici pour l'environnement
      url: ${{ steps.deployment.outputs.page_url }}
    
    steps:
      # --- ÉTAPES D'INSTALLATION ET DÉPLOIEMENT ---
      - name: Récupérer le code
        uses: actions/checkout@v4

      - name: Configurer GitHub Pages
        uses: actions/configure-pages@v4

      - name: Uploader les fichiers
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.'

      - name: Déployer sur GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

      # --- VOS NOUVELLES ÉTAPES DE VÉRIFICATION ---
      
      # 1. On attend un peu que le DNS se propage ou que le serveur démarre
      - name: Attendre que le site soit prêt
        run: sleep 30

      # 2. On teste si le site répond (Code 200 OK)
      - name: Vérifier que le Jeu de la Vie est en ligne
        run: |
          # --fail fait échouer la commande si le code HTTP est >= 400
          # Nous utilisons l'URL fournie par l'étape 'deployment' juste au-dessus
          curl --fail ${{ steps.deployment.outputs.page_url }} || exit 1
          
          echo "Succès : Le Jeu de la Vie est vivant et accessible ! 🧬"